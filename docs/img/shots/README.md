# Screenshots for the user guide

The current toolbar and feature-panel screenshots are also stored in this
folder as `guide-*.png`. They are generated headlessly from the module's own
render harness and referenced by `user-guide.html`; regenerate them after a
material interface change rather than editing them by hand. A few older chart
gallery and wizard screenshots remain embedded as data URIs.
